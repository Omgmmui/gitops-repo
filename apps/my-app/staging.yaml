apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: my-app-staging
  namespace: argocd
  finalizers:
  - resources-finalizer.argocd.argoproj.io
  annotations:
    notifications.argoproj.io/subscribe.on-sync-succeeded.slack: "#argocd-notifications"
    notifications.argoproj.io/subscribe.on-sync-failed.slack: "#argocd-notifications"
    notifications.argoproj.io/subscribe.on-out-of-sync.slack: "#argocd-notifications"
spec:
  project: default
  source:
    repoURL: https://github.com/Omgmmui/gitops-repo.git
    targetRevision: main
    path: overlays/staging
    kustomize:
      images:
      - nginx=nginx:stable
  destination:
    server: https://kubernetes.default.svc
    namespace: staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
    - CreateNamespace=true
    - PruneLast=true
  ignoreDifferences:
  - group: apps
    kind: Deployment
    jsonPointers:
    - /spec/replicas