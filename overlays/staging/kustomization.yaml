apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../charts/my-app

patches:
  - path: patches/deployment-patch.yaml

commonLabels:
  environment: staging

namespace: staging